## TODO: copy this file into .gitconfig in this very same directory
## and fill in the details in .gitconfig
[user]
    name =
    email =

[core]
    editor = vim

[push]
    default = simple

[alias]
    st = status
    co = checkout

## example configs:
## for different github endpoints you can have different username & email
## they only need to have the "[user]" section (see above), i.e.
## TODO: if you want to use this feature, you need to create .gitconfig-amd and .gitconfig-pub
## and uncomment all this:
# AMD GitHub Enterprise configuration
#
## private/enterprise github config
# [includeIf "hasconfig:remote.*.url:*github.amd.com*"]
#    path = ~/.gitconfig-amd
#
## public github account config
# [includeIf "hasconfig:remote.*.url:*github.com*"]
#    path = ~/.gitconfig-pub
